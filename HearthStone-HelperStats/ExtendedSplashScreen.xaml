<Page
    x:Class="HearthStone_HelperStats.ExtendedSplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HearthStone_HelperStats"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid Background="{ThemeResource SystemControlBackgroundAccentBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" Grid.RowSpan="2">
            <Image x:Name="extendedSplashImage" Source="Assets/SplashScreen.png"/>
        </Canvas>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <ProgressRing Name="splashProgressRing" IsActive="True" Width="40" Foreground="White" />
            <TextBlock x:Name="loadingTextBlock" Height="24" Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>
